<Window x:Class="DE.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DE"
        mc:Ignorable="d"
        Title="Главная" 
        Height="550" Width="850"
        Icon="/Resources/logo.ico"
        WindowStartupLocation="CenterScreen"
        Background="White" >

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" LastChildFill="True" Height="50" Background="White">

          
            <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" Margin="10,0">
                <Button Content="Выход"
                       Width="70"
                       Height="28"
                       Click="LogoutButton_Click"
                       Background="#0046C1"
                       Foreground="White"
                       FontWeight="Bold"
                       FontFamily="Segoe UI"
                       BorderThickness="0"
                       VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" 
                        DockPanel.Dock="Left" 
                        Margin="0,0,10,0"
                        VerticalAlignment="Center">
                <Button x:Name="BackButton"
                       Content="Назад"
                       Width="70"
                       Height="28"
                       Click="BackButton_Click"
                       Background="#0046C1"
                       Foreground="White"
                       FontWeight="Bold"
                       FontFamily="Segoe UI"
                       BorderThickness="0"/>
            </StackPanel>
            
            <!-- фио клиента -->
            <TextBlock x:Name="PageTitleText"
                      DockPanel.Dock="Left"
                      VerticalAlignment="Center"
                      HorizontalAlignment="Center"
                      Margin="20,0,0,0"
                      FontWeight="Bold"
                      FontFamily="Segoe UI"
                      FontSize="16"
                      Foreground="Black"/>

            
            <StackPanel Orientation="Horizontal" 
                        DockPanel.Dock="Right" 
                        Margin="0,0,10,0"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Right">

                <!-- роль пользователя -->
                <TextBlock x:Name="UserNameText" 
                          VerticalAlignment="Center"
                          Margin="0,0,15,0"
                          Foreground="Black"
                          FontFamily="Segoe UI"
                          FontWeight="SemiBold"
                          FontSize="14"/>

                <Button x:Name="ProductsNavButton"
                        Content="Услуги"
                        Width="90"
                        Height="28"
                        Margin="5,0"
                        Click="ProductsNavButton_Click"
                        Background="#0046C1"
                        Foreground="White"
                        FontWeight="Bold"
                        FontFamily="Segoe UI"
                        BorderThickness="0"/>

                <Button x:Name="OrdersNavButton"
                    Content="Заказы"
                    Width="90"
                    Height="28"
                    Margin="5,0"
                    Click="OrdersNavButton_Click"
                    Background="#0046C1"
                    Foreground="White"
                    FontWeight="Bold"
                    FontFamily="Segoe UI"
                    BorderThickness="0"/>
            </StackPanel>
        </DockPanel>

        <Frame x:Name="MainFrame" Grid.Row="1" NavigationUIVisibility="Hidden" Background="#E1E6EA" />

    </Grid>
</Window>